<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context=".JsonParserActivity">

    <Button
        android:id="@+id/getClassesButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="getAllClassesClick"
        android:layout_centerHorizontal="true"
        android:text="@string/parse_all_classes_button" />

    <Button
        android:id="@+id/formatClassesButton"
        android:layout_below="@id/getClassesButton"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:onClick="formatAllClassesClick"
        android:layout_centerHorizontal="true"
        android:text="@string/format_json_classes" />

    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/formatClassesButton"
        android:layout_centerHorizontal="true"
        >
        <TextView
            android:id="@+id/jsonResultText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Lorem ipsum dolor"
            android:layout_centerHorizontal="true"
            android:layout_margin="10dip"

            />
    </ScrollView>

</RelativeLayout>
